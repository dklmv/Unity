using UnityEngine;

public class Crickets : MonoBehaviour
{
    // Эта функция вызывается один раз за кадр
    void Update()
    {
        // Выводит слово "crickets" в консоль каждый кадр
        Debug.Log("crickets");
    }
}

